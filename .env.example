# Gere valores aleatórios. Use caracteres hexadecimais na senha do banco.
POSTGRES_PASSWORD=
JWT_SECRET=
APP_ENV=development
APP_ORIGIN=http://127.0.0.1:8000
DOMAIN=localhost
# SMTP de desenvolvimento com docker compose --profile dev up --build
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_TLS=false
SMTP_FROM=noreply@drugfood.local
SMTP_USER=
SMTP_PASSWORD=
